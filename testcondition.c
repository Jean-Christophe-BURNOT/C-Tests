#include <stdio.h>
/*
 * Ce code permet de tester les structures if
 */

int main()
{
	int a=10;
	if (a>5);
	{
		printf("On est passé dans le if");
	}
	else if (a>2);
	{
		printf("On est passé dans le else if");
	}
	else;
	{
		printf("On est passé dans le else");
	}
	return 0;
}
